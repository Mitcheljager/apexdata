<div class="heading">
  <h2 class="heading__title"><%= link_to "Assault Rifles", page_path("assault-rifles") %></h2>
  <div class="heading__line"></div>
</div>

<%= render partial: "item", collection: assault_rifles, as: :item %>

<div class="heading">
  <h2 class="heading__title"><%= link_to "Sub Machine Guns", page_path("sub-machine-guns") %></h2>
  <div class="heading__line"></div>
</div>

<%= render partial: "item", collection: sub_machine_guns, as: :item %>

<div class="heading">
  <h2 class="heading__title"><%= link_to "Pistols", page_path("pistols") %></h2>
  <div class="heading__line"></div>
</div>

<%= render partial: "item", collection: pistols, as: :item %>

<div class="heading">
  <h2 class="heading__title"><%= link_to "Light Machine Guns", page_path("light-machine-guns") %></h2>
  <div class="heading__line"></div>
</div>

<%= render partial: "item", collection: light_machine_guns, as: :item %>

<div class="heading">
  <h2 class="heading__title"><%= link_to "Sniper Rifles", page_path("sniper-rifles") %></h2>
  <div class="heading__line"></div>
</div>

<%= render partial: "item", collection: sniper_rifles, as: :item %>

<div class="heading">
  <h2 class="heading__title"><%= link_to "Shotguns", page_path("shotguns") %></h2>
  <div class="heading__line"></div>
</div>

<%= render partial: "item", collection: shotguns, as: :item %>
