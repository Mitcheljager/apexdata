<% content_for :page_title, "Equipment" %>

<div class="heading">
  <h2 class="heading__title"><%= link_to "Equipment", page_path("equipment") %></h2>
</div>

<div class="items">
  <%= render partial: "pages/item_types/equipment", collection: equipment, as: :item %>
</div>
